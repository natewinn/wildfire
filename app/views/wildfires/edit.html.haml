= form_for @wildfire, url: "/wildfires/#{@wildfire.id}" do |w|
	= render"forms", w: w
= button_to "Delete", "/wildfires/#{@wildfire.id}", {:action => "delete", :method => :delete}
= button_to "Back to List", wildfires_path, method: "get"